<Page
    x:Class="WhisperLive.Views.SessionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WhisperLive.Views"
    Loaded="Page_Loaded">

    <Page.Resources>
        <Style
            x:Key="HorizontalSwipe"
            BasedOn="{StaticResource DefaultListViewItemStyle}"
            TargetType="ListViewItem">
            <Setter Property="Height" Value="64" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Stretch" />
            <Setter Property="BorderThickness" Value="0" />
        </Style>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Margin="24,16,24,8">
            <TextBlock Style="{StaticResource TitleTextBlockStyle}" Text="Saved Sessions" />
            <TextBlock
                x:Name="SubHeaderText"
                Margin="0,4,0,0"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource CaptionTextBlockStyle}" />
        </StackPanel>

        <!-- Empty state -->
        <StackPanel
            x:Name="EmptyState"
            Grid.Row="1"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Spacing="12"
            Visibility="Collapsed">
            <FontIcon
                HorizontalAlignment="Center"
                FontSize="56"
                Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                Glyph="&#xE8A5;" />
            <TextBlock
                HorizontalAlignment="Center"
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Style="{StaticResource BodyTextBlockStyle}"
                Text="No saved sessions yet." />
        </StackPanel>

        <!-- Sessions list -->
        <ListView
            x:Name="SessionsList"
            Grid.Row="1"
            Margin="16,0,16,16"
            ItemContainerStyle="{StaticResource HorizontalSwipe}"
            SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:SessionItemViewModel">
                    <UserControl PointerEntered="Item_PointerEntered" PointerExited="Item_PointerExited">
                        <Grid AutomationProperties.Name="{x:Bind DisplayName}">
                            <SwipeControl x:Name="ItemSwipeControl">
                                <SwipeControl.RightItems>
                                    <SwipeItems Mode="Execute">
                                        <SwipeItem
                                            Background="Red"
                                            Command="{x:Bind DeleteCommand}"
                                            CommandParameter="{x:Bind FilePath}"
                                            Text="Delete">
                                            <SwipeItem.IconSource>
                                                <FontIconSource Glyph="&#xE74D;" />
                                            </SwipeItem.IconSource>
                                        </SwipeItem>
                                    </SwipeItems>
                                </SwipeControl.RightItems>

                                <Grid Padding="12,0" VerticalAlignment="Center">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <!-- SRT file icon -->
                                    <FontIcon
                                        Grid.Column="0"
                                        Margin="0,0,12,0"
                                        VerticalAlignment="Center"
                                        FontSize="20"
                                        Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                                        Glyph="&#xE8A5;" />

                                    <!-- Display name + subtitle -->
                                    <StackPanel
                                        Grid.Column="1"
                                        VerticalAlignment="Center"
                                        Spacing="2">
                                        <TextBlock
                                            Style="{StaticResource BodyStrongTextBlockStyle}"
                                            Text="{x:Bind DisplayName}" />
                                        <TextBlock
                                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                            Style="{StaticResource CaptionTextBlockStyle}"
                                            Text="{x:Bind SubTitle}" />
                                    </StackPanel>

                                    <!-- Hover action buttons (mouse/pen only) -->
                                    <StackPanel
                                        x:Name="HoverButtons"
                                        Grid.Column="2"
                                        Orientation="Horizontal"
                                        Visibility="Collapsed">
                                        <AppBarButton
                                            Width="48"
                                            Command="{x:Bind OpenCommand}"
                                            CommandParameter="{x:Bind FilePath}"
                                            IsTabStop="False"
                                            Label="Open"
                                            LabelPosition="Collapsed"
                                            ToolTipService.ToolTip="View transcript">
                                            <AppBarButton.Icon>
                                                <FontIcon Glyph="&#xE890;" />
                                            </AppBarButton.Icon>
                                        </AppBarButton>
                                        <AppBarButton
                                            Width="48"
                                            Command="{x:Bind DeleteCommand}"
                                            CommandParameter="{x:Bind FilePath}"
                                            IsTabStop="False"
                                            Label="Delete"
                                            LabelPosition="Collapsed"
                                            ToolTipService.ToolTip="Delete session">
                                            <AppBarButton.Icon>
                                                <FontIcon Glyph="&#xE74D;" />
                                            </AppBarButton.Icon>
                                        </AppBarButton>
                                    </StackPanel>
                                </Grid>
                            </SwipeControl>

                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="HoveringStates">
                                    <VisualState x:Name="HoverButtonsHidden" />
                                    <VisualState x:Name="HoverButtonsShown">
                                        <VisualState.Setters>
                                            <Setter Target="HoverButtons.Visibility" Value="Visible" />
                                        </VisualState.Setters>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                        </Grid>
                    </UserControl>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
