<Page
    x:Class="WhisperBurner.WinUI.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WhisperBurner.WinUI.Views"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer Padding="24">
        <StackPanel Spacing="16" MaxWidth="600">
            <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" />

            <Expander Header="Transcription Backend" IsExpanded="True">
                <StackPanel Padding="0,8" Spacing="8">
                    <TextBox
                        x:Name="ApiUrlBox"
                        Header="Docker API URL"
                        PlaceholderText="http://localhost:5000" />
                    <ComboBox x:Name="ModelCombo" Header="Whisper model" SelectedIndex="2">
                        <ComboBoxItem Content="tiny" />
                        <ComboBoxItem Content="base" />
                        <ComboBoxItem Content="small" />
                        <ComboBoxItem Content="medium" />
                        <ComboBoxItem Content="large-v3" />
                        <ComboBoxItem Content="turbo" />
                    </ComboBox>
                </StackPanel>
            </Expander>

            <Expander Header="Recording" IsExpanded="True">
                <StackPanel Padding="0,8" Spacing="8">
                    <ToggleSwitch
                        x:Name="CaptureSystemAudioToggle"
                        Header="Capture system audio (recommended for YouTube/video)"
                        IsOn="True" />
                    <ToggleSwitch
                        x:Name="CaptureMicToggle"
                        Header="Capture microphone instead"
                        IsOn="False" />
                    <NumberBox
                        x:Name="ChunkDurationBox"
                        Header="Chunk duration (seconds)"
                        Value="3"
                        Minimum="1"
                        Maximum="15"
                        SpinButtonPlacementMode="Inline" />
                </StackPanel>
            </Expander>

            <Expander Header="Sessions storage">
                <StackPanel Padding="0,8" Spacing="8">
                    <TextBox x:Name="SessionsPathBox" Header="Sessions folder" IsReadOnly="True" />
                </StackPanel>
            </Expander>

            <Button
                Content="Save settings"
                Style="{StaticResource AccentButtonStyle}"
                Click="Save_Click" />

            <TextBlock x:Name="SaveStatusText" Text="" FontSize="12"
                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>
    </ScrollViewer>
</Page>
