<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Red stop button — BasedOn omitted: WinUI 3 implicit DefaultButtonStyle is not
         reachable via {StaticResource} from a merged app dictionary. Colors set here are
         overrides; visual states inherit from the TargetType implicit style at runtime. -->
    <Style x:Key="StopButtonStyle" TargetType="Button">
        <Setter Property="Background" Value="#C42B1C" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="BorderBrush" Value="#C42B1C" />
    </Style>

    <!-- Waveform bar — 4px wide rounded pill, accent fill -->
    <Style x:Key="WaveformBarStyle" TargetType="Border">
        <Setter Property="Width" Value="4" />
        <Setter Property="Height" Value="32" />
        <Setter Property="CornerRadius" Value="2" />
        <Setter Property="Background" Value="{ThemeResource SystemAccentColorBrush}" />
    </Style>

    <!-- Recording-active dot colour -->
    <SolidColorBrush x:Key="RecordingActiveBrush" Color="#C42B1C" />

</ResourceDictionary>
